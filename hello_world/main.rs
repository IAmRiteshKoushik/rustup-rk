fn main() {
    // The main function takes no parameters and returns nothing
    println!("Hello World!");
    // Using a ! means that you are calling a macro and not a function
    // To be discussed in detail later on.
}
